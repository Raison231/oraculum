{from:from||new Date(),days:days,birth:'1998-04-10'});const tb=document.querySelector('#predictTable tbody');tb.innerHTML='';p.calendar.forEach(r=>{const cls=r.overall>=78?'peak':(r.moneyScore>=90?'money':'');tb.innerHTML+=`<tr class="${cls}"><td>${h(r.date)}</td><td>${r.dr}</td><td>${h(r.luna)}</td><td>${r.overall}</td><td>${r.rings}</td><td>${h(r.money)}</td><td>${h(r.verdict)}</td></tr>`;});document.getElementById('predictBest').innerHTML=`<div class="kv">${kv('Запуск',p.best.launch.date)}${kv('Деньги',p.best.money.date)}${kv('Релиз',p.best.release.date)}${kv('Сделка',p.best.deal.date)}${kv('Deep Work',p.best.deepWork.date)}</div>`;document.getElementById('predictBest').className='result show';}
function runCreate(){const i=document.getElementById('cIntent').value;const c=ORC.createDesign({intent:i});const el=document.getElementById('createResult');if(!c.ok){el.innerHTML='<div class="empty">'+h(c.message)+'</div>';return;}el.innerHTML=`<div class="kv">${kv('DR',c.dr)}${kv('Ось',c.ax